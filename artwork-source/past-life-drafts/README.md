# Past-life draft assets

These files are retained as source material so the full creative work remains
available on every development computer. They are intentionally stored outside
`public/`: the production experience keeps the user-selected original entrance
film, and no current route references these alternate drafts.

Moving them out of `public/` prevents Vite and Vercel from copying roughly
34 MB of unused media into every production deployment.
